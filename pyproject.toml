[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "optimization-foundations"
version = "0.1.0"
description = "A hands-on LP, SDP, and MILP learning repository"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["experiments"]

